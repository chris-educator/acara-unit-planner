{
  "name": "acara-unit-planner",
  "private": true,
  "scripts": {
    "dev:api": "uvicorn server.main:app --reload --port 8026",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client"
  }
}
